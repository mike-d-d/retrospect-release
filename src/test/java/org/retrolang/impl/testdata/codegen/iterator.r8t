
return [firstTwo(withKeys([1.1, 1.2, 1.3])), firstTwo(withKeys([2.1, 2.2, 2.3]))]

function firstTwo(array) {
  it = iterator(array, EnumerateValues)
  first = next(it=)
  second = next(it=)
  return { first, second }
}

/* CODEGEN firstTwo RETURNS
[{first: [[1], 1.1], second: [[2], 1.2]}, {first: [[1], 2.1], second: [[2], 2.2]}]
---
firstTwo_0(WithKeysMatrix([d1, d2, d3])):
 1: AtomicInteger.incrementAndGet(0);
 2: TState.setResultTemplates(X0, [Struct(["first", "second"], [[[1], d0], [[2], d8]])]);
 3: X5 ← TState.fnResultBytes(X0, 16);
 4: setDouble[](X5, 0, D1);
 5: setDouble[](X5, 8, D3);
 6: return
---
allocated=776/32, peak=528
*/